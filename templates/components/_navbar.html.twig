<nav class="navbar fixed-top navbar-expand-md navbar-dark bg-dark bg-navbar">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('app_home') }}">
            <img class="img-fluid mx-2" src="" alt="logo" width="40">
        </a>
        <button class="navbar-toggler border-secondary" type="button" data-bs-toggle="collapse"
            data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false"
            aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul
                class="navbar-nav d-flex justify-content-between w-100 align-lg-items-center gap-lg-4 pt-4 pt-md-0 pb-2 pb-md-0">
                <div class="d-flex flex-column flex-md-row">
                    <li class="nav-item px-2">
                        <a class="nav-link letter-spacing" href="#">Jeux</a>
                    </li>
                    <li class="nav-item px-2">
                        <a class="nav-link letter-spacing" href="#">Catégorie</a>
                    </li>
                </div>
            </ul>
            {% if app.user %}
				<div class="d-flex flex-column flex-md-row align-items-md-center position-relative pe-3">
					{% include '/components/svg/_icon_profile.svg.twig' %}
					<button class="custom-btn dropdown-toggle d-flex align-items-center ms-1" type="button"
						data-bs-toggle="dropdown" aria-expanded="false">
						<p class="m-0">
							<span class="text-uppercase">{{ app.user.firstname }}</span>
						</p>
					</button>
                </div>
            {% else %}
					<ul class="navbar-nav mr-auto ms-4 ms-sm-0 mb-4 mb-sm-0">
						<li class="nav-item d-flex align-items-end align-items-center px-2">
							<a class="nav-link text-uppercase letter-spacing" href="{{ path('app_login') }}">
								{% include '/components/svg/_icon_profile.svg.twig' %}
								Connexion
							</a>
						</li>
					</ul>
            {% endif %}
        </div>
    </div>
</nav>